import 'package:flutter/material.dart';


class MyAcountView extends StatelessWidget {
  const MyAcountView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text('My Account'),),
    );
  }
}