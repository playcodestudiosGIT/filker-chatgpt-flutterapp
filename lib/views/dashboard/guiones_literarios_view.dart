import 'package:flutter/material.dart';


class GuionesLiterariosView extends StatelessWidget {
  const GuionesLiterariosView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text('Guiones Literarios'),),
    );
  }
}